apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  labels:
    app.kubernetes.io/name: app
type: Opaque
stringData:
  # Provide actual values via CI or kubectl apply -f with overrides
  JWT_SECRET: ""

