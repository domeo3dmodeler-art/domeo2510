apiVersion: batch/v1
kind: Job
metadata:
  name: domeo-migrations
  namespace: domeo-staging
spec:
  template:
    spec:
      containers:
      - name: migrations
        image: cr.yandex/your-registry/domeo:staging-latest
        command: ["npx", "prisma", "migrate", "deploy"]
        env:
        - name: DATABASE_URL
          value: "postgresql://domeo_admin:jbx-R8B-PST-d8x@c9q9lthigo2ncgm3orfe.mdb.yandexcloud.net:6432/domeo"
        - name: NODE_ENV
          value: "staging"
      restartPolicy: Never
  backoffLimit: 3
