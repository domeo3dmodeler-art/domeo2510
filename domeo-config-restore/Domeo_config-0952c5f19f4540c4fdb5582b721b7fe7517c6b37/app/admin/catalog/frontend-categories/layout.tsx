'use client';

import React from 'react';

export default function FrontendCategoriesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div>
      {children}
    </div>
  );
}
