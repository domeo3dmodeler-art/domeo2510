# ✅ НАСТРОЙКА GIT СИНХРОНИЗАЦИИ ЗАВЕРШЕНА

**Дата:** 2025-01-05

---

## ✅ ЧТО СДЕЛАНО

1. ✅ Инициализирован git репозиторий локально
2. ✅ Настроен remote на GitHub: `https://github.com/domeo3dmodeler-art/domeo2510.git`
3. ✅ Создана ветка `develop`
4. ✅ Созданы скрипты синхронизации:
   - `scripts/git-sync-to-staging.ps1` - для Windows
   - `scripts/git-sync-to-staging.sh` - для Linux/Mac
   - `scripts/update-staging.sh` - для запуска на ВМ
5. ✅ Создана документация: `docs/GIT_SYNC_WORKFLOW.md`

---

## 🚀 БЫСТРЫЙ СТАРТ

### 1. Первый коммит (если нужно)

```powershell
git add .
git commit -m "Initial sync: настройка git workflow"
git push -u origin develop
```

### 2. Синхронизация изменений

```powershell
.\scripts\git-sync-to-staging.ps1 "Описание изменений"
```

**Что делает скрипт:**
- ✅ Добавляет все изменения
- ✅ Создает коммит
- ✅ Отправляет на GitHub
- ✅ Обновляет код на staging ВМ (git pull)
- ✅ Перезапускает контейнер для hot reload

---

## 📋 ПАЙПЛАЙН РАЗРАБОТКИ

```
Локальная разработка
    ↓
.\scripts\git-sync-to-staging.ps1 "Описание"
    ↓
git push origin develop
    ↓
Обновление на ВМ (git pull)
    ↓
Hot Reload (перезапуск контейнера)
    ↓
Изменения видны на http://130.193.40.35:3001
```

---

## 🔧 РЕКОМЕНДАЦИИ

1. **Всегда коммитьте изменения перед синхронизацией**
2. **Используйте описательные сообщения коммитов**
3. **Проверяйте изменения на staging после синхронизации**

---

## 📚 ДОКУМЕНТАЦИЯ

Полная документация: `docs/GIT_SYNC_WORKFLOW.md`

---

**Готово к использованию!**

