# 🚀 ПРОДОЛЖЕНИЕ РАЗРАБОТКИ С HOT RELOAD

## ✅ Статус: ГОТОВО К РАЗРАБОТКЕ!

Hot Reload настроен и работает. Вы можете продолжать разработку!

---

## 📋 БЫСТРЫЙ WORKFLOW РАЗРАБОТКИ:

### 1. Измените код локально

Редактируйте файлы в:
- `app/` - страницы и API routes
- `components/` - React компоненты
- `lib/` - утилиты и библиотеки

### 2. Синхронизируйте с staging

```powershell
# Быстрая синхронизация одной директории
scp -r app ubuntu@130.193.40.35:/opt/domeo/

# Или синхронизируйте все измененные директории
scp -r app components lib ubuntu@130.193.40.35:/opt/domeo/
```

### 3. Перезапустите контейнер (если нужно)

```powershell
ssh ubuntu@130.193.40.35 'cd /opt/domeo && docker compose -f docker-compose.staging-dev.yml restart staging-app'
```

**Примечание:** В dev режиме Next.js автоматически перезагружает изменения, но иногда нужен перезапуск контейнера для применения изменений в конфигурации или зависимостях.

### 4. Проверьте изменения

Откройте в браузере: **http://130.193.40.35:3001**

---

## 🔄 РАЗНЫЕ СЦЕНАРИИ:

### Сценарий 1: Изменение React компонента

```powershell
# 1. Измените компонент локально (например: components/Button.tsx)
# 2. Синхронизируйте
scp -r components ubuntu@130.193.40.35:/opt/domeo/
# 3. Изменения применятся автоматически через Hot Reload!
```

### Сценарий 2: Изменение API route

```powershell
# 1. Измените API route локально (например: app/api/orders/route.ts)
# 2. Синхронизируйте
scp -r app/api ubuntu@130.193.40.35:/opt/domeo/app/
# 3. Перезапустите контейнер
ssh ubuntu@130.193.40.35 'cd /opt/domeo && docker compose -f docker-compose.staging-dev.yml restart staging-app'
```

### Сценарий 3: Изменение библиотеки/утилиты

```powershell
# 1. Измените библиотеку локально (например: lib/utils.ts)
# 2. Синхронизируйте
scp -r lib ubuntu@130.193.40.35:/opt/domeo/
# 3. Перезапустите контейнер
ssh ubuntu@130.193.40.35 'cd /opt/domeo && docker compose -f docker-compose.staging-dev.yml restart staging-app'
```

### Сценарий 4: Изменение конфигурации (package.json, next.config.mjs)

```powershell
# 1. Измените конфигурацию локально
# 2. Скопируйте файл
scp package.json ubuntu@130.193.40.35:/opt/domeo/
scp next.config.mjs ubuntu@130.193.40.35:/opt/domeo/
# 3. Перезапустите контейнер (переустановит зависимости)
ssh ubuntu@130.193.40.35 'cd /opt/domeo && docker compose -f docker-compose.staging-dev.yml restart staging-app'
```

---

## 📊 ПОЛЕЗНЫЕ КОМАНДЫ:

### Просмотр логов в реальном времени:

```powershell
ssh ubuntu@130.193.40.35 'docker logs -f domeo-staging-app'
```

### Статус контейнеров:

```powershell
ssh ubuntu@130.193.40.35 'docker ps | grep staging'
```

### Проверка health check:

```powershell
Invoke-WebRequest -Uri "http://130.193.40.35:3001/api/health" -UseBasicParsing
```

---

## ⚡ ОПТИМИЗАЦИЯ:

### Автоматизация синхронизации:

Можете создать простой скрипт для быстрой синхронизации:

```powershell
# sync-app.ps1
scp -r app ubuntu@130.193.40.35:/opt/domeo/
ssh ubuntu@130.193.40.35 'cd /opt/domeo && docker compose -f docker-compose.staging-dev.yml restart staging-app'
```

Использование:
```powershell
.\sync-app.ps1
```

---

## 🎯 РЕКОМЕНДАЦИИ:

1. **Используйте Git** - коммитьте изменения перед синхронизацией
2. **Тестируйте локально** - используйте `npm run dev` для быстрой проверки
3. **Синхронизируйте часто** - небольшие изменения проще отлаживать
4. **Следите за логами** - проверяйте логи при ошибках

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ:

### Проблема: Изменения не применяются

**Решение:**
```powershell
# Перезапустите контейнер
ssh ubuntu@130.193.40.35 'cd /opt/domeo && docker compose -f docker-compose.staging-dev.yml restart staging-app'
```

### Проблема: Ошибки в логах

**Решение:**
```powershell
# Проверьте логи
ssh ubuntu@130.193.40.35 'docker logs --tail=50 domeo-staging-app'
```

### Проблема: Контейнер не запускается

**Решение:**
```powershell
# Пересоздайте контейнер
ssh ubuntu@130.193.40.35 'cd /opt/domeo && docker compose -f docker-compose.staging-dev.yml down && docker compose -f docker-compose.staging-dev.yml up -d'
```

---

## 📝 СЛЕДУЮЩИЕ ШАГИ:

1. ✅ Hot Reload настроен - **ГОТОВО**
2. 🔄 Начните разработку - изменяйте код локально
3. 📤 Синхронизируйте изменения - используйте `scp` команды
4. 🧪 Тестируйте на staging - проверяйте в браузере
5. 💾 Коммитьте изменения - сохраняйте в Git

---

**Готово к разработке!** 🚀

Теперь у вас быстрый цикл разработки без долгого ожидания пересборки Docker образа!

