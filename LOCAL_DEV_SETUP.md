# 🚀 ПОЛНАЯ НАСТРОЙКА ЛОКАЛЬНОЙ РАЗРАБОТКИ

## ⚡ Быстрый старт (3 команды)

### Терминал 1: SSH туннель
```powershell
npm run dev:tunnel
```
> Оставьте этот терминал открытым!

### Терминал 2: Dev сервер
```powershell
npm run dev
```

### Браузер
Откройте: `http://localhost:3000`

---

## 📋 Подробная инструкция

### Шаг 1: Проверка .env.local

Убедитесь, что файл `.env.local` существует и содержит:

```env
DATABASE_URL="postgresql://staging_user:staging_password@localhost:5432/domeo_staging?schema=public"
NEXTAUTH_SECRET="local-dev-secret-change-in-production"
NEXTAUTH_URL="http://localhost:3000"
```

Если файла нет, создайте его:
```powershell
# Используйте автоматический скрипт
npm run dev:quick
```

### Шаг 2: Запуск SSH туннеля

**Вариант А: Автоматический (рекомендуется)**
```powershell
npm run dev:tunnel
```

**Вариант Б: Ручной**
```powershell
ssh -L 5432:localhost:5432 ubuntu@130.193.40.35
```

> 💡 **Важно:** Оставьте терминал с туннелем открытым!

### Шаг 3: Запуск dev сервера

В **новом терминале**:
```powershell
npm run dev
```

### Шаг 4: Проверка

1. Откройте браузер: `http://localhost:3000`
2. Проверьте, что приложение загрузилось
3. Внесите изменение в код → увидите hot reload

---

## 🔧 Полезные команды

```powershell
# Запустить SSH туннель
npm run dev:tunnel

# Запустить dev сервер
npm run dev

# Быстрая настройка (проверит .env.local и запустит dev)
npm run dev:quick

# Сгенерировать Prisma клиент
npm run prisma:generate

# Открыть Prisma Studio (GUI для БД)
npm run prisma:studio

# Проверить типы TypeScript
npm run type-check

# Проверить линтер
npm run lint
```

---

## ✅ Проверка подключения

### Проверка SSH туннеля
```powershell
netstat -an | findstr 5432
```

Должно показать: `TCP    127.0.0.1:5432`

### Проверка подключения к БД
```powershell
npm run prisma:studio
```

Если открывается Prisma Studio → подключение работает!

---

## 🐛 Troubleshooting

### Проблема: "Port 5432 is already in use"

**Решение:**
1. Найдите процесс, использующий порт:
   ```powershell
   netstat -ano | findstr :5432
   ```
2. Завершите процесс или используйте другой локальный порт

### Проблема: "Connection refused" при подключении к БД

**Решение:**
1. Проверьте, что SSH туннель запущен
2. Проверьте подключение к ВМ: `ssh ubuntu@130.193.40.35`
3. Перезапустите туннель

### Проблема: Prisma клиент не обновился

**Решение:**
```powershell
npm run prisma:generate
# Перезапустите dev сервер (Ctrl+C и npm run dev)
```

---

## 🎯 Workflow разработки

1. **Откройте терминал 1** → `npm run dev:tunnel` (оставьте открытым)
2. **Откройте терминал 2** → `npm run dev`
3. **Редактируйте код** → изменения применяются автоматически
4. **Проверяйте результат** в браузере на `http://localhost:3000`
5. **После завершения** → коммитьте и деплойте на staging

---

## 📝 Важные замечания

1. **НЕ используйте `prisma migrate`** для staging БД - это может повредить данные
2. **Для изменений схемы** используйте только `prisma generate` - миграции применяются на ВМ при деплое
3. **После изменений в schema.prisma** всегда запускайте `npm run prisma:generate`
4. **Не удаляйте данные** в staging БД во время разработки

---

## 🎉 Готово!

Теперь у вас настроена быстрая локальная разработка с hot reload!

**Что дальше:**
- Редактируйте код → изменения применяются мгновенно
- Используйте debugger, console.log, React DevTools
- Работайте с реальными данными из staging
- Не нужно пересобирать Docker образ!

