# 🚀 Канонический подход к разработке с Cursor

## 📋 Оглавление
1. [Философия работы](#философия-работы)
2. [Процесс разработки](#процесс-разработки)
3. [Работа с UI](#работа-с-ui)
4. [Работа с базой данных](#работа-с-базой-данных)
5. [Работа с API](#работа-с-api)
6. [Внедрение новых функций](#внедрение-новых-функций)
7. [Чек-листы](#чек-листы)
8. [Best Practices](#best-practices)

---

## 🎯 Философия работы

### Принципы
1. **Системный подход** - всегда думать о всей системе, а не только о текущей задаче
2. **Инкрементальные изменения** - маленькие, проверяемые шаги
3. **Быстрая обратная связь** - тестировать после каждого изменения
4. **Документирование** - фиксировать важные решения
5. **Переиспользование** - искать существующие решения перед созданием новых

### Правило 3-х шагов
1. **Анализ** - понять контекст и зависимости
2. **Планирование** - разбить на шаги
3. **Реализация** - выполнить с проверкой

---

## 🔄 Процесс разработки

### Этап 1: Анализ задачи

**Вопросы для анализа:**
- [ ] Что именно нужно сделать? (конкретная формулировка)
- [ ] Где это используется в системе?
- [ ] Какие компоненты/модули затронуты?
- [ ] Есть ли похожие реализации в проекте?
- [ ] Какие зависимости (UI, БД, API)?
- [ ] Какие могут быть побочные эффекты?

**Инструменты:**
```bash
# Поиск существующих решений
codebase_search: "Как реализовано X?"
grep: "поиск паттернов"
glob_file_search: "поиск похожих файлов"
```

### Этап 2: Планирование

**Создание TODO списка:**
```typescript
todo_write({
  merge: false,
  todos: [
    { id: 'task-1', status: 'pending', content: 'Описание задачи 1' },
    { id: 'task-2', status: 'pending', content: 'Описание задачи 2' },
  ]
})
```

**Разбивка на этапы:**
1. Подготовка (создание файлов, структуры)
2. Реализация (код)
3. Интеграция (связывание с существующим кодом)
4. Тестирование (проверка работы)
5. Документирование (комментарии, README)

### Этап 3: Реализация

**Порядок действий:**
1. Сначала читаем существующий код
2. Потом создаем/изменяем
3. Сразу проверяем линтер
4. Тестируем функциональность
5. Коммитим с понятным сообщением

---

## 🎨 Работа с UI

### Стратегия изменений UI

#### 1. Создание нового компонента

**Шаги:**
```bash
# 1. Ищем похожие компоненты
codebase_search: "Как реализован компонент Y?"

# 2. Проверяем структуру папок
list_dir: "components/"

# 3. Создаем компонент на основе паттерна
write: "components/new/NewComponent.tsx"

# 4. Сразу проверяем линтер
read_lints: ["components/new/NewComponent.tsx"]
```

**Правила:**
- ✅ Использовать существующие UI компоненты (`@/components/ui`)
- ✅ Следовать существующим паттернам стилизации
- ✅ Делать компонент переиспользуемым
- ❌ Не создавать дубликаты существующих компонентов

#### 2. Изменение существующего UI

**Порядок действий:**
1. **Читаем весь файл** - понимаем структуру
2. **Ищем все использования** - `grep` по имени компонента
3. **Планируем изменения** - что меняем, что может сломаться
4. **Делаем изменения** - маленькими шагами
5. **Проверяем все места использования** - не сломали ли что-то

**Пример:**
```typescript
// 1. Читаем файл
read_file: "components/ClientForm.tsx"

// 2. Ищем использования
grep: "ClientForm"

// 3. Планируем
todo_write: ["изменить поле X", "обновить валидацию", "проверить все формы"]

// 4. Меняем
search_replace: "старое -> новое"

// 5. Проверяем
read_lints: ["components/ClientForm.tsx"]
grep: "ClientForm" // проверяем, что все импорты правильные
```

#### 3. Унификация UI компонентов

**Когда нужно унифицировать:**
- Одинаковая функциональность в разных местах
- Похожие формы/модалки
- Дублирование кода

**Процесс:**
1. Находим все варианты реализации
2. Выбираем лучший как эталон
3. Создаем единый компонент
4. Заменяем все использования
5. Удаляем старый код

**Пример (как мы делали с CreateClientModal):**
```typescript
// 1. Находим все формы создания клиента
grep: "createClient|newClient"

// 2. Выбираем эталон (форма из корзины)
read_file: "app/doors/page.tsx" // форма из корзины

// 3. Создаем единый компонент
write: "components/clients/CreateClientModal.tsx"

// 4. Заменяем везде
search_replace: "старая форма -> CreateClientModal"

// 5. Удаляем дубликаты
delete_file: "старые формы" (если были отдельные файлы)
```

---

## 🗄️ Работа с базой данных

### Стратегия изменений БД

#### 1. Изменение схемы Prisma

**Порядок действий:**
```bash
# 1. Читаем текущую схему
read_file: "prisma/schema.prisma"

# 2. Ищем все использования поля/модели
grep: "ModelName|fieldName"

# 3. Планируем миграцию
# - Какие поля добавляем/удаляем/меняем
# - Нужна ли миграция данных
# - Обратная совместимость

# 4. Меняем схему
search_replace: "prisma/schema.prisma"

# 5. Обновляем типы
run_terminal_cmd: "npx prisma generate"

# 6. Создаем миграцию (если нужно)
run_terminal_cmd: "npx prisma migrate dev --name description"
```

**Правила:**
- ✅ Всегда проверять обратную совместимость
- ✅ Использовать `optional()` для новых полей
- ✅ Добавлять `@default()` для обязательных полей
- ❌ Не удалять поля без миграции данных
- ❌ Не менять типы без проверки использования

#### 2. Изменение репозиториев

**Порядок:**
1. Читаем репозиторий
2. Ищем все использования методов
3. Меняем репозиторий
4. Обновляем все места использования
5. Обновляем типы/схемы валидации

**Пример:**
```typescript
// 1. Читаем
read_file: "lib/repositories/client.repository.ts"

// 2. Ищем использования
grep: "clientRepository.create|clientRepository.update"

// 3. Меняем интерфейс/реализацию
search_replace: "старый метод -> новый"

// 4. Обновляем схемы валидации
read_file: "lib/validation/client.schemas.ts"
search_replace: "старая схема -> новая"

// 5. Обновляем API
read_file: "app/api/clients/route.ts"
search_replace: "старый вызов -> новый"

// 6. Проверяем все места
grep: "CreateClientInput|UpdateClientInput"
read_lints: ["lib/repositories/client.repository.ts", "app/api/clients/route.ts"]
```

---

## 🔌 Работа с API

### Стратегия изменений API

#### 1. Изменение существующего эндпоинта

**Порядок:**
1. Читаем текущий роут
2. Ищем все клиентские вызовы
3. Планируем изменения (обратная совместимость?)
4. Меняем API
5. Обновляем клиентский код
6. Тестируем

**Пример:**
```typescript
// 1. Читаем
read_file: "app/api/clients/route.ts"

// 2. Ищем вызовы
grep: "fetch.*\/api\/clients|fetchWithAuth.*clients"

// 3. Меняем
search_replace: "старая логика -> новая"

// 4. Обновляем клиентов
grep: "fetch.*\/api\/clients"
// обновляем каждый вызов

// 5. Проверяем
read_lints: ["app/api/clients/route.ts"]
```

#### 2. Создание нового эндпоинта

**Шаблон:**
```typescript
// 1. Создаем файл по паттерну существующих
read_file: "app/api/clients/route.ts" // как пример

// 2. Создаем новый роут
write: "app/api/new-endpoint/route.ts"

// 3. Добавляем валидацию
read_file: "lib/validation/client.schemas.ts"
// создаем схему валидации

// 4. Добавляем в репозиторий (если нужно)
read_file: "lib/repositories/client.repository.ts"
// добавляем метод

// 5. Тестируем
read_lints: ["app/api/new-endpoint/route.ts"]
```

---

## 🆕 Внедрение новых функций

### Процесс внедрения новой функции

#### Этап 1: Анализ и планирование

```bash
# 1. Понимаем требования
# - Что должна делать функция?
# - Где она будет использоваться?
# - Какие данные нужны?

# 2. Ищем похожие реализации
codebase_search: "Как реализована похожая функция X?"

# 3. Планируем архитектуру
# - Какие компоненты нужны?
# - Какие API эндпоинты?
# - Какие изменения в БД?
# - Какие UI компоненты?

# 4. Создаем TODO
todo_write: {
  todos: [
    { id: 'db', status: 'pending', content: 'Создать миграцию БД' },
    { id: 'api', status: 'pending', content: 'Создать API эндпоинты' },
    { id: 'ui', status: 'pending', content: 'Создать UI компоненты' },
    { id: 'integration', status: 'pending', content: 'Интегрировать все части' },
  ]
}
```

#### Этап 2: Реализация (снизу вверх)

**Порядок:**
1. **База данных** (если нужно)
   - Схема Prisma
   - Миграция
   - Типы

2. **Репозиторий/Сервис**
   - Бизнес-логика
   - Работа с БД

3. **API**
   - Эндпоинты
   - Валидация
   - Обработка ошибок

4. **UI**
   - Компоненты
   - Интеграция с API
   - Обработка состояний

5. **Интеграция**
   - Связывание всех частей
   - Тестирование потока

#### Этап 3: Тестирование

**Чек-лист:**
- [ ] Линтер не показывает ошибок
- [ ] Функция работает в изоляции
- [ ] Интеграция с существующим кодом работает
- [ ] Нет регрессий (старое не сломалось)
- [ ] UI выглядит правильно
- [ ] Обработка ошибок работает

---

## ✅ Чек-листы

### Чек-лист для изменения UI компонента

- [ ] Прочитал весь файл компонента
- [ ] Нашел все места использования (`grep`)
- [ ] Понял зависимости и пропсы
- [ ] Проверил существующие UI компоненты для переиспользования
- [ ] Внес изменения
- [ ] Проверил линтер (`read_lints`)
- [ ] Проверил все места использования (не сломал ли что-то)
- [ ] Протестировал визуально (если возможно)
- [ ] Обновил типы (если изменил интерфейс)

### Чек-лист для изменения БД

- [ ] Прочитал схему Prisma
- [ ] Нашел все использования модели/поля (`grep`)
- [ ] Понял влияние на существующий код
- [ ] Спланировал миграцию данных (если нужно)
- [ ] Изменил схему
- [ ] Сгенерировал типы (`npx prisma generate`)
- [ ] Обновил репозитории
- [ ] Обновил схемы валидации
- [ ] Обновил API (если нужно)
- [ ] Обновил клиентский код (если нужно)
- [ ] Проверил линтер
- [ ] Протестировал миграцию (если есть)

### Чек-лист для изменения API

- [ ] Прочитал текущий роут
- [ ] Нашел все клиентские вызовы (`grep`)
- [ ] Понял обратную совместимость
- [ ] Обновил валидацию (если нужно)
- [ ] Изменил API
- [ ] Обновил клиентский код (если нужно)
- [ ] Проверил линтер
- [ ] Протестировал эндпоинт

### Чек-лист перед коммитом

- [ ] Все изменения работают локально
- [ ] Линтер не показывает ошибок
- [ ] Нет неиспользуемого кода
- [ ] Коммит-сообщение понятное и описательное
- [ ] Изменения логически связаны (один коммит = одна задача)
- [ ] Проверил, что не сломал существующую функциональность

### Чек-лист перед деплоем

- [ ] Все коммиты запушены
- [ ] Изменения применены на VM
- [ ] Приложение перезапущено
- [ ] Проверил работу на staging
- [ ] Нет критических ошибок в логах

---

## 🎯 Best Practices

### 1. Работа с Cursor

**DO:**
- ✅ Использовать `codebase_search` для понимания контекста
- ✅ Читать файлы полностью перед изменением
- ✅ Использовать `grep` для поиска всех использований
- ✅ Создавать TODO списки для сложных задач
- ✅ Проверять линтер после каждого изменения
- ✅ Делать маленькие, проверяемые изменения

**DON'T:**
- ❌ Менять код без понимания контекста
- ❌ Игнорировать существующие паттерны
- ❌ Создавать дубликаты кода
- ❌ Коммитить без проверки линтера
- ❌ Делать большие изменения одним коммитом

### 2. Структура кода

**Правила:**
- Один файл = одна ответственность
- Компоненты должны быть переиспользуемыми
- Использовать существующие утилиты и хелперы
- Следовать существующим паттернам именования

### 3. Работа с типами

**Правила:**
- Всегда обновлять типы при изменении интерфейсов
- Использовать типы из схем валидации
- Не использовать `any` без необходимости
- Проверять типы через линтер

### 4. Обработка ошибок

**Правила:**
- Всегда обрабатывать ошибки в API
- Показывать понятные сообщения пользователю
- Логировать ошибки для отладки
- Использовать существующие паттерны обработки ошибок

### 5. Производительность

**Правила:**
- Не делать лишних запросов к API
- Использовать кеширование где возможно
- Оптимизировать рендеринг (useMemo, useCallback)
- Проверять размер бандла

---

## 📝 Шаблоны команд для Cursor

### Анализ задачи
```
codebase_search: "Как реализована функция X?"
grep: "паттерн поиска"
glob_file_search: "**/*Component*.tsx"
list_dir: "components/"
```

### Изменение компонента
```
read_file: "components/Component.tsx"
grep: "Component"
search_replace: "старое -> новое"
read_lints: ["components/Component.tsx"]
```

### Изменение БД
```
read_file: "prisma/schema.prisma"
grep: "ModelName"
search_replace: "старая схема -> новая"
run_terminal_cmd: "npx prisma generate"
```

### Изменение API
```
read_file: "app/api/endpoint/route.ts"
grep: "fetch.*endpoint"
search_replace: "старая логика -> новая"
read_lints: ["app/api/endpoint/route.ts"]
```

---

## 🚀 Быстрый старт для новой задачи

1. **Анализ** (5 минут)
   - Понять задачу
   - Найти похожие реализации
   - Понять зависимости

2. **Планирование** (5 минут)
   - Создать TODO список
   - Разбить на этапы
   - Определить порядок действий

3. **Реализация** (зависит от задачи)
   - Следовать чек-листам
   - Делать маленькие шаги
   - Проверять после каждого шага

4. **Тестирование** (5-10 минут)
   - Проверить линтер
   - Протестировать функциональность
   - Проверить интеграцию

5. **Деплой** (2 минуты)
   - Закоммитить
   - Запушить
   - Применить на VM

---

## 📚 Дополнительные ресурсы

- [Документация Prisma](https://www.prisma.io/docs)
- [Next.js API Routes](https://nextjs.org/docs/api-routes/introduction)
- [React Best Practices](https://react.dev/learn)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)

---

## 🔄 Итеративное улучшение

Этот документ должен обновляться по мере накопления опыта. После каждой крупной задачи:
- Что сработало хорошо?
- Что можно улучшить?
- Какие новые паттерны появились?
- Какие проблемы возникли?

**Обновляйте этот документ!**

