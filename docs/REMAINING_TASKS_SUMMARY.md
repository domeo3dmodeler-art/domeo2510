# 📋 Что еще нужно сделать

**Дата:** 2025-11-09  
**Версия системы:** develop

## ✅ Уже выполнено

1. ✅ Исправлено несоответствие статусов SupplierOrder в типах
2. ✅ Унифицирована валидация переходов статусов
3. ✅ Улучшена проверка прав доступа для смены статусов
4. ✅ Добавлена аутентификация к экспорту Excel
5. ✅ Проведен аудит работоспособности всех ЛК
6. ✅ Заменен DEPRECATED endpoint `/api/applications?executor_id=...` на `/api/orders?executor_id=...` в `ApplicationsBoard.tsx`

---

## ⚠️ Основные задачи, которые остались

### 1. Заменить DEPRECATED endpoints в `ApplicationsBoard.tsx` (приоритет: средний)

**Проблема:**
В `components/executor/ApplicationsBoard.tsx` все еще используются DEPRECATED endpoints:

- `GET /api/applications/${id}` → нужно заменить на `GET /api/orders/${id}`
- `POST /api/applications/${id}/project` → нужно проверить, есть ли актуальный endpoint
- `POST /api/applications/${id}/files` → нужно проверить, есть ли актуальный endpoint
- `PUT /api/applications/${id}/status` → нужно заменить на `PUT /api/orders/${id}/status`
- `PUT /api/applications/${id}/door-dimensions` → нужно проверить, есть ли актуальный endpoint
- `POST /api/applications/${id}/verify` → нужно проверить, есть ли актуальный endpoint

**Действие:**
1. Проверить наличие актуальных endpoints для всех операций
2. Заменить все вызовы на актуальные endpoints
3. Протестировать функциональность

**Статус:** ⚠️ Требует проверки и замены

---

### 2. Удалить DEPRECATED endpoints после замены (приоритет: низкий)

**Проблема:**
DEPRECATED endpoints все еще существуют в коде:

- `app/api/applications/[id]/status/route.ts`
- `app/api/applications/[id]/route.ts`
- `app/api/applications/[id]/door-dimensions/route.ts`
- `app/api/applications/[id]/verify/route.ts`
- `app/api/applications/[id]/project/route.ts`
- `app/api/applications/[id]/files/route.ts`
- `app/api/applications/route.ts`

**Действие:**
1. Убедиться, что все DEPRECATED endpoints заменены
2. Удалить DEPRECATED endpoints
3. Обновить документацию

**Статус:** ⚠️ Требует проверки использования

---

### 3. Добавить проверки обязательных полей при смене статуса (приоритет: средний)

**Проблема:**
Только для перехода в `UNDER_REVIEW` проверяется наличие `project_file_url`. Для других переходов нет проверок обязательных полей.

**Примеры:**
- При переходе в `AWAITING_MEASUREMENT` может потребоваться проверка `measurement_done`
- При переходе в `COMPLETED` может потребоваться проверка всех обязательных полей

**Действие:**
Создать функцию для проверки обязательных полей:

```typescript
export function validateStatusTransitionRequirements(
  documentType: string,
  currentStatus: string,
  newStatus: string,
  document: any
): { valid: boolean; error?: string } {
  // Проверки для каждого перехода
}
```

**Статус:** ⚠️ Требует реализации

---

### 4. Унифицировать документацию статусов Order (приоритет: низкий)

**Проблема:**
В разных документах описаны разные цепочки статусов Order.

**Действие:**
1. Обновить документацию согласно коду
2. Удалить устаревшие описания
3. Создать единый источник истины для документации

**Статус:** ⚠️ Требует обновления документации

---

### 5. Унифицировать обработку ответов API (приоритет: низкий)

**Проблема:**
Некоторые компоненты ожидают прямой ответ, но API возвращает `{ success: true, data: ... }`.

**Действие:**
Создать helper функцию для обработки ответов API:

```typescript
export function parseApiResponse<T>(response: any): T | null {
  if (response && typeof response === 'object' && 'data' in response) {
    return response.data as T;
  }
  return null;
}
```

**Статус:** ⚠️ Частично исправлено, требуется унификация

---

## 🎯 Рекомендуемый порядок выполнения

### Сейчас (средний приоритет)
1. Заменить DEPRECATED endpoints в `ApplicationsBoard.tsx`
2. Добавить проверки обязательных полей при смене статуса

### Позже (низкий приоритет)
3. Удалить DEPRECATED endpoints после замены
4. Унифицировать документацию статусов Order
5. Унифицировать обработку ответов API
6. Добавить unit-тесты
7. Добавить интеграционные тесты

---

## 📝 Примечания

- DEPRECATED endpoints оставлены для обратной совместимости
- Перед удалением нужно убедиться, что они не используются
- Документация может быть обновлена позже, так как код является источником истины
- Тесты можно добавить постепенно

---

**Конец списка задач**

