# 🔍 Аудит работоспособности всех ЛК (Личных Кабинетов)

**Дата аудита:** 2025-11-09  
**Версия системы:** develop

## 📋 Содержание

1. [Обзор ЛК](#обзор-лк)
2. [ЛК Администратора](#лк-администратора)
3. [ЛК Комплектатора](#лк-комплектатора)
4. [ЛК Исполнителя](#лк-исполнителя)
5. [ЛК Менеджера](#лк-менеджера)
6. [Общие проблемы](#общие-проблемы)
7. [Рекомендации](#рекомендации)

---

## 📊 Обзор ЛК

### Роли пользователей

1. **ADMIN** - Администратор
2. **COMPLECTATOR** - Комплектатор
3. **EXECUTOR** - Исполнитель
4. **MANAGER** - Менеджер/Руководитель

### Структура ЛК

- **`app/dashboard/page.tsx`** - Главная страница дашборда (роутер по ролям)
- **`app/complectator/dashboard/`** - ЛК комплектатора
- **`app/executor/dashboard/`** - ЛК исполнителя
- **`app/manager/dashboard/`** - ЛК менеджера
- **`app/admin/`** - ЛК администратора (разные страницы)

---

## 👤 ЛК Администратора

### Файлы

- **`app/dashboard/page.tsx`** - Главная страница (роутинг)
- **`app/admin/users/page.tsx`** - Управление пользователями
- **`app/admin/categories/`** - Управление категориями
- **`app/admin/import/`** - Импорт прайсов
- **`app/admin/catalog/`** - Управление каталогом

### Функциональность

✅ **Работает:**
- Просмотр статистики
- Управление пользователями
- Управление категориями
- Импорт прайсов
- Управление каталогом

⚠️ **Проблемы:**
- Нет единой страницы дашборда администратора
- Используется общий роутер в `app/dashboard/page.tsx`
- Статистика загружается из `/api/admin/stats` (требует роль ADMIN)

### API Endpoints

- `GET /api/admin/stats` - Статистика (требует роль ADMIN)
- `GET /api/users` - Список пользователей (требует роль ADMIN)
- `GET /api/admin/categories` - Категории
- `POST /api/admin/import/universal` - Универсальный импорт

### Проверка работоспособности

- ✅ Загрузка статистики
- ✅ Управление пользователями
- ✅ Импорт прайсов
- ✅ Управление категориями

---

## 👤 ЛК Комплектатора

### Файлы

- **`app/complectator/dashboard/page.tsx`** - Главная страница
- **`app/complectator/dashboard/ComplectatorDashboardComponent.tsx`** - Основной компонент

### Функциональность

✅ **Работает:**
- Просмотр клиентов
- Просмотр заказов
- Создание клиентов
- Создание заказов
- Изменение статусов заказов (DRAFT → SENT → PAID)
- Экспорт счетов и КП в PDF
- Модальное окно деталей заказа

⚠️ **Проблемы:**
- Использует DEPRECATED endpoint `/api/applications` (через `OrdersBoard`)
- Нет проверки прав доступа для изменения статусов исполнителя
- Экспорт PDF может не работать из-за отсутствия Chromium (исправлено)

### API Endpoints

- `GET /api/complectator/stats` - Статистика комплектатора
- `GET /api/clients` - Список клиентов
- `GET /api/orders` - Список заказов
- `GET /api/orders/[id]` - Детали заказа
- `PUT /api/orders/[id]/status` - Изменение статуса заказа
- `POST /api/export/fast` - Экспорт PDF

### Проверка работоспособности

- ✅ Загрузка клиентов
- ✅ Загрузка заказов
- ✅ Создание клиентов
- ✅ Создание заказов
- ✅ Изменение статусов заказов
- ✅ Модальное окно заказа
- ✅ Экспорт PDF (после исправления Chromium)

---

## 👤 ЛК Исполнителя

### Файлы

- **`app/executor/dashboard/page.tsx`** - Главная страница
- **`components/executor/OrdersBoard.tsx`** - Компонент заказов
- **`components/executor/ApplicationsBoard.tsx`** - Компонент заявок (DEPRECATED)

### Функциональность

✅ **Работает:**
- Просмотр клиентов
- Просмотр счетов
- Просмотр заказов у поставщика
- Создание заказов у поставщика
- Экспорт Excel заказов у поставщика
- Изменение статусов заказов (NEW_PLANNED → UNDER_REVIEW → ...)
- Изменение статусов заказов у поставщика

⚠️ **Проблемы:**
- Использует DEPRECATED endpoint `/api/applications` в `ApplicationsBoard.tsx`
- Экспорт Excel не был защищен аутентификацией (исправлено)
- Нет проверки прав доступа для изменения статусов комплектатора

### API Endpoints

- `GET /api/clients` - Список клиентов
- `GET /api/clients/[id]` - Детали клиента
- `GET /api/invoices` - Список счетов
- `GET /api/supplier-orders` - Список заказов у поставщика
- `GET /api/supplier-orders/[id]/excel` - Экспорт Excel (исправлено - добавлена аутентификация)
- `PUT /api/supplier-orders/[id]/status` - Изменение статуса заказа у поставщика
- `GET /api/orders?executor_id=...` - Список заказов исполнителя
- `PUT /api/orders/[id]/status` - Изменение статуса заказа

### Проверка работоспособности

- ✅ Загрузка клиентов
- ✅ Загрузка счетов
- ✅ Загрузка заказов у поставщика
- ✅ Создание заказов у поставщика
- ✅ Экспорт Excel заказов у поставщика (после исправления)
- ✅ Изменение статусов заказов
- ✅ Изменение статусов заказов у поставщика

---

## 👤 ЛК Менеджера

### Файлы

- **`app/manager/dashboard/page.tsx`** - Главная страница
- **`app/manager/dashboard/ManagerDashboardComponent.tsx`** - Основной компонент

### Функциональность

✅ **Работает:**
- Просмотр статистики
- Просмотр заказов с фильтрами
- Фильтрация по статусу, комплектатору, исполнителю, датам
- Модальное окно деталей заказа

⚠️ **Проблемы:**
- Менеджер не может изменять статусы (только просмотр) - это правильно
- Нет проверки прав доступа для изменения статусов (но менеджер не должен изменять)

### API Endpoints

- `GET /api/manager/stats` - Статистика менеджера
- `GET /api/orders?manager_id=...` - Список заказов с фильтрами
- `GET /api/orders/[id]` - Детали заказа

### Проверка работоспособности

- ✅ Загрузка статистики
- ✅ Загрузка заказов
- ✅ Фильтрация заказов
- ✅ Модальное окно заказа

---

## 🔧 Общие проблемы

### 1. Использование DEPRECATED endpoints

**Проблема:**
- `components/executor/ApplicationsBoard.tsx` использует `/api/applications?executor_id=...`
- `components/executor/OrdersBoard.tsx` может использовать `/api/applications`

**Решение:**
- Заменить на `/api/orders?executor_id=...`
- Удалить DEPRECATED endpoints после замены

### 2. Отсутствие аутентификации в некоторых endpoints

**Проблема:**
- `GET /api/supplier-orders/[id]/excel` не был защищен (исправлено)

**Решение:**
- Добавить `requireAuth` ко всем endpoints (исправлено)

### 3. Неправильная обработка ответов API

**Проблема:**
- Некоторые компоненты ожидают прямой ответ, но API возвращает `{ success: true, data: ... }`

**Решение:**
- Унифицировать обработку ответов API (частично исправлено)

### 4. Отсутствие проверки прав доступа

**Проблема:**
- Комплектатор может попытаться изменить статус исполнителя
- Исполнитель может попытаться изменить статус комплектатора

**Решение:**
- Улучшить проверку прав доступа в `canUserChangeStatus`

---

## 💡 Рекомендации

### 1. Заменить DEPRECATED endpoints

**Действие:**
1. Заменить `/api/applications` на `/api/orders` в `ApplicationsBoard.tsx`
2. Удалить DEPRECATED endpoints после замены

### 2. Улучшить проверку прав доступа

**Действие:**
Создать матрицу прав доступа для всех переходов статусов:

```typescript
const STATUS_CHANGE_PERMISSIONS = {
  order: {
    'DRAFT → SENT': ['ADMIN', 'COMPLECTATOR'],
    'SENT → PAID': ['ADMIN', 'COMPLECTATOR'],
    'PAID → UNDER_REVIEW': ['ADMIN', 'COMPLECTATOR'],
    'UNDER_REVIEW → AWAITING_MEASUREMENT': ['ADMIN', 'EXECUTOR'],
    // ...
  }
};
```

### 3. Унифицировать обработку ответов API

**Действие:**
Создать helper функцию для обработки ответов API:

```typescript
export function parseApiResponse<T>(response: any): T | null {
  if (response && typeof response === 'object' && 'data' in response) {
    return response.data as T;
  }
  return null;
}
```

### 4. Добавить проверки обязательных полей

**Действие:**
Добавить проверки обязательных полей при смене статуса:

```typescript
export function validateStatusTransitionRequirements(
  documentType: string,
  currentStatus: string,
  newStatus: string,
  document: any
): { valid: boolean; error?: string } {
  // Проверки для каждого перехода
}
```

---

## ✅ Чек-лист проверки ЛК

### ЛК Администратора

- [x] Загрузка статистики
- [x] Управление пользователями
- [x] Импорт прайсов
- [x] Управление категориями
- [x] Управление каталогом

### ЛК Комплектатора

- [x] Загрузка клиентов
- [x] Загрузка заказов
- [x] Создание клиентов
- [x] Создание заказов
- [x] Изменение статусов заказов
- [x] Модальное окно заказа
- [x] Экспорт PDF

### ЛК Исполнителя

- [x] Загрузка клиентов
- [x] Загрузка счетов
- [x] Загрузка заказов у поставщика
- [x] Создание заказов у поставщика
- [x] Экспорт Excel заказов у поставщика
- [x] Изменение статусов заказов
- [x] Изменение статусов заказов у поставщика

### ЛК Менеджера

- [x] Загрузка статистики
- [x] Загрузка заказов
- [x] Фильтрация заказов
- [x] Модальное окно заказа

---

## 📚 Связанные файлы

### ЛК Комплектатора

1. `app/complectator/dashboard/page.tsx`
2. `app/complectator/dashboard/ComplectatorDashboardComponent.tsx`
3. `components/complectator/OrderDetailsModal.tsx`

### ЛК Исполнителя

1. `app/executor/dashboard/page.tsx`
2. `components/executor/OrdersBoard.tsx`
3. `components/executor/ApplicationsBoard.tsx` (DEPRECATED)

### ЛК Менеджера

1. `app/manager/dashboard/page.tsx`
2. `app/manager/dashboard/ManagerDashboardComponent.tsx`

### ЛК Администратора

1. `app/dashboard/page.tsx`
2. `app/admin/users/page.tsx`
3. `app/admin/categories/`
4. `app/admin/import/`

---

## 🎯 Приоритеты исправлений

### Высокий приоритет

1. ✅ Исправить несоответствие статусов SupplierOrder
2. ✅ Добавить аутентификацию к экспорту Excel
3. ✅ Унифицировать валидацию переходов статусов
4. ⚠️ Заменить DEPRECATED endpoints

### Средний приоритет

5. Улучшить проверку прав доступа
6. Унифицировать обработку ответов API
7. Добавить проверки обязательных полей

### Низкий приоритет

8. Удалить DEPRECATED endpoints
9. Добавить unit-тесты
10. Добавить интеграционные тесты

---

**Конец отчета аудита ЛК**

