# 📋 Оставшиеся задачи

> 💡 **Важно:** Перед началом работы с задачами изучи [CURSOR_DEVELOPMENT_METHODOLOGY.md](./CURSOR_DEVELOPMENT_METHODOLOGY.md) и [QUICK_REFERENCE.md](./QUICK_REFERENCE.md) для ускорения разработки.

**Дата:** 2025-11-11  
**Версия системы:** develop

## ✅ Выполнено

1. ✅ Исправлено несоответствие статусов SupplierOrder в типах
2. ✅ Унифицирована валидация переходов статусов (удалено дублирование)
3. ✅ Улучшена проверка прав доступа для смены статусов
4. ✅ Добавлена аутентификация к экспорту Excel
5. ✅ Проведен аудит работоспособности всех ЛК
6. ✅ Заменен DEPRECATED endpoint в `ApplicationsBoard.tsx`
7. ✅ Убраны блоки загрузки "Тех. задания" и "Оптовые счета" для комплектатора (2025-11-11)
   - Комплектатор теперь видит только блок "Проект/планировка" для загрузки файлов
   - Блоки "Тех. задания" и "Оптовые счета" остались только для исполнителя
8. ✅ Добавлена кнопка удаления заказа в ЛК Комплектатора (2025-11-11)
   - Кнопка с иконкой корзины справа от каждого заказа в списке
   - Модальное окно подтверждения удаления
   - Автоматическое обновление списка после удаления
9. ✅ Удалены все DEPRECATED endpoints `/api/applications/*` (2025-11-11)
   - Проверено использование в коде - не используются
   - Все компоненты используют актуальные endpoints `/api/orders/*`
   - Удалено 7 файлов DEPRECATED endpoints
10. ✅ Улучшены проверки обязательных полей при смене статуса (2025-11-11)
   - Добавлены проверки для статуса `READY_FOR_PRODUCTION`
   - Исправлено дублирование логики в `app/api/orders/[id]/route.ts`
   - Теперь используется унифицированная функция `validateStatusTransitionRequirements` везде
11. ✅ Унифицирована обработка ответов API в ApplicationsBoard.tsx (2025-11-11)
   - Заменен ручной парсинг на `parseApiResponse` в трех местах
   - Улучшена консистентность обработки ответов API
12. ✅ Исправлены критические места с прямым `fetch` (2025-11-11)
   - Заменено 21 место на `fetchWithAuth` в критических компонентах:
     - `components/executor/OrdersBoard.tsx` - 1 место
     - `app/complectator/dashboard/ComplectatorDashboardComponent.tsx` - 7 мест
     - `app/executor/dashboard/page.tsx` - 3 места
     - `lib/cart/price-recalculation-service.ts` - 2 места
     - `app/doors/page.tsx` - 7 мест
   - Улучшена безопасность и консистентность авторизации
13. ✅ Проведен полный аудит проекта (2025-11-11)
   - Проверена обработка ошибок в API endpoints (260+ используют `withErrorHandling`)
   - Проверена валидация входных данных (критические endpoints используют Zod)
   - Созданы отчеты: `docs/FULL_PROJECT_AUDIT.md` и `docs/AUDIT_SUMMARY.md`

## ⚠️ Осталось сделать

### 1. Удалить DEPRECATED endpoints (приоритет: средний) ✅ ВЫПОЛНЕНО

**Проблема:**
DEPRECATED endpoints все еще существуют и могут использоваться:

**Статус:** ✅ **ВЫПОЛНЕНО** (2025-11-11)
- ✅ Проверено использование в коде - не используются
- ✅ Проверено наличие актуальных endpoints - все существуют
- ✅ Удалены все DEPRECATED endpoints:
  - ✅ `app/api/applications/route.ts`
  - ✅ `app/api/applications/[id]/route.ts`
  - ✅ `app/api/applications/[id]/status/route.ts`
  - ✅ `app/api/applications/[id]/door-dimensions/route.ts`
  - ✅ `app/api/applications/[id]/verify/route.ts`
  - ✅ `app/api/applications/[id]/project/route.ts`
  - ✅ `app/api/applications/[id]/files/route.ts`

**Результат:** Все компоненты используют актуальные endpoints `/api/orders/*`. DEPRECATED endpoints удалены.

---

### 2. Унифицировать документацию статусов Order (приоритет: низкий)

**Проблема:**
В разных документах описаны разные цепочки статусов Order:

- `docs/SYSTEM_RULES_AND_PERMISSIONS.md` - `DRAFT → CONFIRMED → IN_PRODUCTION → READY → COMPLETED`
- `docs/ORDER_STATUS_CHAIN.md` - `DRAFT → SENT → PAID → UNDER_REVIEW → ...`
- `docs/DOCUMENT_LOGIC_COMPLETE.md` - `NEW_PLANNED → UNDER_REVIEW → ...`

**Действие:**
1. Обновить документацию согласно коду (`lib/validation/status-transitions.ts`)
2. Удалить устаревшие описания
3. Создать единый источник истины для документации

**Статус:** ⚠️ Требует обновления документации

---

### 3. Добавить проверки обязательных полей при смене статуса (приоритет: средний) ✅ ВЫПОЛНЕНО

**Проблема:**
Только для перехода в `UNDER_REVIEW` проверялось наличие `project_file_url`. Для других переходов не было проверок обязательных полей.

**Статус:** ✅ **ВЫПОЛНЕНО** (2025-11-11)
- ✅ Добавлены проверки для статуса `READY_FOR_PRODUCTION`
- ✅ Исправлено дублирование логики в `app/api/orders/[id]/route.ts`
- ✅ Теперь используется унифицированная функция `validateStatusTransitionRequirements` везде
- ✅ Проверки покрывают все важные переходы:
  - `UNDER_REVIEW` - требуется `project_file_url`
  - `AWAITING_MEASUREMENT` - требуется `project_file_url`
  - `AWAITING_INVOICE` - требуется `project_file_url` и `door_dimensions`
  - `READY_FOR_PRODUCTION` - требуется `project_file_url` и `door_dimensions`
  - `COMPLETED` - требуется `project_file_url` и `door_dimensions`

**Результат:** Все переходы статусов теперь проверяют обязательные поля через единую функцию.

---

### 4. Унифицировать обработку ответов API (приоритет: низкий) ✅ ЧАСТИЧНО ВЫПОЛНЕНО

**Проблема:**
Некоторые компоненты ожидают прямой ответ, но API возвращает `{ success: true, data: ... }`.

**Статус:** ✅ **ЧАСТИЧНО ВЫПОЛНЕНО** (2025-11-11)
- ✅ Функция `parseApiResponse` уже существует и широко используется
- ✅ Исправлен ручной парсинг в `ApplicationsBoard.tsx` (3 места)
- ⚠️ Остались места в конструкторе (`ProductCatalogBlock.tsx`, `DetailedBlockEditor.tsx`), но они не критичны

**Результат:** Основные компоненты используют `parseApiResponse`. Остальные места можно исправить постепенно.

---

### 5. Добавить unit-тесты для валидации переходов (приоритет: низкий)

**Действие:**
1. Создать тесты для `canTransitionTo`
2. Создать тесты для `canUserChangeStatus`
3. Создать тесты для проверки обязательных полей

**Статус:** ⚠️ Требует создания тестов

---

### 6. Добавить интеграционные тесты для API endpoints (приоритет: низкий)

**Действие:**
1. Создать тесты для `/api/orders/[id]/status`
2. Создать тесты для `/api/supplier-orders/[id]/status`
3. Создать тесты для проверки прав доступа

**Статус:** ⚠️ Требует создания тестов

---

## 🎯 Приоритеты

### Высокий приоритет
- Нет задач с высоким приоритетом

### Средний приоритет
1. Удалить DEPRECATED endpoints (после проверки использования)
2. Добавить проверки обязательных полей при смене статуса

### Низкий приоритет
3. Унифицировать документацию статусов Order
4. Унифицировать обработку ответов API
5. Добавить unit-тесты
6. Добавить интеграционные тесты

---

## 📝 Примечания

- DEPRECATED endpoints оставлены для обратной совместимости
- Перед удалением нужно убедиться, что они не используются
- Документация может быть обновлена позже, так как код является источником истины
- Тесты можно добавить постепенно

---

**Конец списка задач**

