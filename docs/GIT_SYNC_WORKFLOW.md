# 🔄 WORKFLOW СИНХРОНИЗАЦИИ ЧЕРЕЗ GIT

**Цель:** Быстрая синхронизация локальной разработки с staging ВМ без пересборки Docker образа

---

## 📋 ПАЙПЛАЙН РАЗРАБОТКИ

```
Локальная разработка
    ↓
git add + commit
    ↓
git push origin develop
    ↓
Обновление на staging ВМ (git pull)
    ↓
Hot Reload (перезапуск контейнера)
    ↓
Изменения видны на http://130.193.40.35:3001
```

---

## 🚀 БЫСТРЫЙ СТАРТ

### Вариант 1: Автоматическая синхронизация (рекомендуется)

**Windows PowerShell:**
```powershell
.\scripts\git-sync-to-staging.ps1 "Описание изменений"
```

**Linux/Mac:**
```bash
chmod +x scripts/git-sync-to-staging.sh
./scripts/git-sync-to-staging.sh "Описание изменений"
```

**Что делает скрипт:**
1. ✅ Проверяет изменения в git
2. ✅ Добавляет все изменения (`git add -A`)
3. ✅ Создает коммит с указанным сообщением
4. ✅ Отправляет на GitHub (`git push origin develop`)
5. ✅ Обновляет код на staging ВМ (`git pull`)
6. ✅ Перезапускает контейнер для hot reload

### Вариант 2: Ручная синхронизация

**Локально:**
```bash
git add .
git commit -m "Описание изменений"
git push origin develop
```

**На staging ВМ:**
```bash
ssh ubuntu@130.193.40.35 'cd /opt/domeo && git pull origin develop && docker compose -f docker-compose.staging-dev.yml restart staging-app'
```

Или используйте скрипт на ВМ:
```bash
ssh ubuntu@130.193.40.35 'cd /opt/domeo && ./scripts/update-staging.sh'
```

---

## 📝 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### С автосообщением коммита:
```powershell
.\scripts\git-sync-to-staging.ps1
# Использует автоматическое сообщение: "Auto-sync: 2025-01-05 10:30:45"
```

### С указанным сообщением:
```powershell
.\scripts\git-sync-to-staging.ps1 "Исправлена ошибка логирования"
```

### Без коммита (только push существующих коммитов):
```powershell
.\scripts\git-sync-to-staging.ps1 -NoCommit
```

### Без push (только локальный коммит):
```powershell
.\scripts\git-sync-to-staging.ps1 -NoPush
```

---

## 🔧 НАСТРОЙКА

### Первоначальная настройка локально:

1. **Инициализация git (если еще не сделано):**
   ```bash
   git init
   git remote add origin https://github.com/domeo3dmodeler-art/domeo2510.git
   git branch -M develop
   ```

2. **Проверка remote:**
   ```bash
   git remote -v
   # Должно показать:
   # origin  https://github.com/domeo3dmodeler-art/domeo2510.git (fetch)
   # origin  https://github.com/domeo3dmodeler-art/domeo2510.git (push)
   ```

3. **Первый push (если нужно):**
   ```bash
   git add .
   git commit -m "Initial commit"
   git push -u origin develop
   ```

### Настройка на staging ВМ:

Уже настроено! На ВМ:
- ✅ Git репозиторий в `/opt/domeo`
- ✅ Remote настроен на `origin https://github.com/domeo3dmodeler-art/domeo2510.git`
- ✅ Ветка `develop` настроена

---

## ⚡ HOT RELOAD НА STAGING

Staging использует **Hot Reload режим** через Docker Compose:

**Конфигурация:** `docker-compose.staging-dev.yml`

**Особенности:**
- ✅ Весь код монтируется как volume (`.:/app`)
- ✅ Next.js запущен в dev режиме (`npm run dev`)
- ✅ Изменения применяются автоматически при `git pull`
- ✅ Перезапуск контейнера применяет изменения мгновенно

**После `git pull` на ВМ:**
```bash
# Перезапуск контейнера (применяет изменения)
docker compose -f docker-compose.staging-dev.yml restart staging-app

# Или используйте скрипт
./scripts/update-staging.sh
```

---

## 🔍 ПРОВЕРКА СТАТУСА

### Локально:
```bash
git status
git log --oneline -5
```

### На staging ВМ:
```bash
ssh ubuntu@130.193.40.35 'cd /opt/domeo && git status'
ssh ubuntu@130.193.40.35 'cd /opt/domeo && git log --oneline -5'
```

### Проверка контейнера:
```bash
ssh ubuntu@130.193.40.35 'cd /opt/domeo && docker compose -f docker-compose.staging-dev.yml logs --tail=20 staging-app'
```

---

## 🛠️ УСТРАНЕНИЕ ПРОБЛЕМ

### Проблема: "fatal: not a git repository"
**Решение:**
```bash
git init
git remote add origin https://github.com/domeo3dmodeler-art/domeo2510.git
```

### Проблема: "Permission denied (publickey)"
**Решение:** Настройте SSH ключ для GitHub:
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
# Добавьте публичный ключ в GitHub Settings > SSH Keys
```

### Проблема: "Updates were rejected because the remote contains work"
**Решение:**
```bash
git pull origin develop
# Разрешите конфликты если есть
git push origin develop
```

### Проблема: Изменения не применяются на staging
**Решение:**
1. Проверьте что `git pull` выполнен успешно
2. Перезапустите контейнер:
   ```bash
   ssh ubuntu@130.193.40.35 'cd /opt/domeo && docker compose -f docker-compose.staging-dev.yml restart staging-app'
   ```
3. Проверьте логи:
   ```bash
   ssh ubuntu@130.193.40.35 'cd /opt/domeo && docker compose -f docker-compose.staging-dev.yml logs --tail=50 staging-app'
   ```

---

## 📊 ПРЕИМУЩЕСТВА

### ✅ Git синхронизация:
- История изменений
- Откат к предыдущим версиям
- Работа в команде
- Бэкап кода

### ✅ Hot Reload:
- Изменения видны за секунды
- Нет пересборки Docker образа (экономия времени)
- Автоматическое применение изменений

### ✅ Автоматизация:
- Один скрипт для всего процесса
- Меньше ручных действий
- Меньше ошибок

---

## 🔄 РЕКОМЕНДУЕМЫЙ WORKFLOW

1. **Разрабатываете локально:**
   ```bash
   # Вносите изменения в файлы
   # Тестируете локально (npm run dev)
   ```

2. **Синхронизируете изменения:**
   ```powershell
   .\scripts\git-sync-to-staging.ps1 "Описание изменений"
   ```

3. **Проверяете на staging:**
   - Откройте http://130.193.40.35:3001
   - Проверьте что изменения применены

4. **При необходимости откатываете:**
   ```bash
   git log --oneline -10  # Найти коммит
   git revert <commit-hash>  # Откатить
   .\scripts\git-sync-to-staging.ps1 "Откат изменений"
   ```

---

**Обновлено:** 2025-01-05

