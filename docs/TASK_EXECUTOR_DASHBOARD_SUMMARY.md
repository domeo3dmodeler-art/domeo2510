# Краткое резюме: Управление заявками в личном кабинете исполнителя

## 🎯 Суть задачи

Добавить в личный кабинет исполнителя систему управления заявками (приложениями) с отслеживанием по статусам и обязательной загрузкой проектов с размерами дверей.

## 📐 Текущее состояние vs Требуемое

### Сейчас:
- Левая панель: клиенты
- Правая панель: вкладки "Счета" и "Заказ у поставщика" (документы выбранного клиента)

### Нужно:
- Левая панель: клиенты (без изменений)
- Правая панель: **Табло заявок** с вкладками по статусам:
  1. Новая заявка (запланированная)
  2. На проверке
  3. Ждут замер
  4. Ожидают счет (Подгрузить оптовые счета)
  5. Заявка выполнена (счета подгружены)

## 🔑 Ключевые требования

### 1. Статусы заявок
```
NEW_PLANNED → UNDER_REVIEW → [AWAITING_MEASUREMENT | AWAITING_INVOICE] → COMPLETED
```

### 2. Обязательная загрузка проекта
- При переходе в статус "На проверке" требуется наличие проекта/планировки
- Форматы: PDF, JPG, PNG, DWG, DXF
- Блокировка перехода без файла

### 3. Проверка данных дверей
- Сравнение размеров из счета с размерами из проекта
- Проверка количества дверей
- Определение стороны открывания (левая/правая)
- Указание количества заверток
- Без проверки нельзя запросить счет

### 4. Логика запроса счета
- **Если замер был или проект простой** → сразу запросить счет
- **Если замера не было или проект сложный** → направить на замер → после замера запросить счет

### 5. Загрузка файлов
- **Оптовые счета**: PDF, Excel (для поставщиков)
- **Техзадания на проемы**: PDF (для передачи прорабам)
- Обязательны в статусе "Ожидают счет"

## 📊 Структура данных

**Новая модель `Application`:**
- Связь с клиентом (`client_id`)
- Связь со счетом (`invoice_id`)
- Статус заявки
- URL проекта (`project_file_url`) - **обязательно**
- Данные дверей (JSON: размеры, количество, открывание, завертки)
- Файлы: оптовые счета, техзадания

## 🔄 Логика работы

1. **Создание заявки:**
   - Автоматически при оплате счета (статус → `PAID`)
   - Статус новой заявки: `NEW_PLANNED`

2. **Начало обработки:**
   - Переход в `UNDER_REVIEW`
   - **Требуется загрузка проекта** (блокировка без файла)

3. **Проверка:**
   - Сравнение данных дверей: счет vs проект
   - Определение необходимости замера

4. **Решение о замере:**
   - Простой проект или замер был → `AWAITING_INVOICE`
   - Сложный проект без замера → `AWAITING_MEASUREMENT` → после замера → `AWAITING_INVOICE`

5. **Загрузка счетов:**
   - В статусе `AWAITING_INVOICE` загрузить:
     - Оптовые счета
     - Техзадания на проемы (PDF)

6. **Завершение:**
   - После загрузки всех файлов → `COMPLETED`

## 🎨 UI Компоненты

### Табло заявок
- Горизонтальные вкладки по статусам
- Счетчики заявок на вкладках
- Таблица с колонками: ДАТА, ЛИД, КОМПЛЕКТАТОР, ФИО КЛИЕНТА, Адрес

### Модальные окна
1. **Загрузка проекта** - обязательная форма
2. **Проверка данных дверей** - сравнение и редактирование
3. **Определение замера** - выбор действия (замер / запрос счета)
4. **Загрузка файлов** - оптовые счета + техзадания

## ✅ Критерии приемки

1. ✅ Модель `Application` создана в БД
2. ✅ API endpoints реализованы
3. ✅ Табло заявок с вкладками работает
4. ✅ Обязательная загрузка проекта с валидацией
5. ✅ Проверка данных дверей реализована
6. ✅ Логика определения замера работает
7. ✅ Загрузка оптовых счетов и техзаданий работает
8. ✅ Автоматическое создание при оплате счета

## 📝 Связанные документы

- Полное ТЗ: `docs/TASK_EXECUTOR_DASHBOARD_APPLICATIONS.md`
- Архитектура: `docs/ARCHITECTURE.md`
- Правила системы: `docs/SYSTEM_RULES_AND_PERMISSIONS.md`

---

**Приоритет:** Высокий  
**Статус:** В ожидании разработки


