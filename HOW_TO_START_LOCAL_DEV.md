# 🚀 ЗАПУСК ЛОКАЛЬНОЙ РАЗРАБОТКИ - ПОШАГОВАЯ ИНСТРУКЦИЯ

## ⚠️ ВАЖНО: Нужно запустить ДВА терминала!

---

## 📋 ИНСТРУКЦИЯ

### ТЕРМИНАЛ 1: SSH туннель (запустите сначала!)

1. Откройте **новый терминал PowerShell**
2. Выполните:
   ```powershell
   cd c:\01_conf\2410_deploy
   npm run dev:tunnel
   ```
3. Вы увидите что-то вроде:
   ```
   🔐 Запуск SSH туннеля к staging БД...
   🔗 Устанавливаю SSH туннель...
   ```
4. **ОСТАВЬТЕ ЭТОТ ТЕРМИНАЛ ОТКРЫТЫМ!** Туннель работает пока терминал открыт.

### ТЕРМИНАЛ 2: Dev сервер (запустите после терминала 1!)

1. Откройте **еще один новый терминал PowerShell**
2. Выполните:
   ```powershell
   cd c:\01_conf\2410_deploy
   npm run dev
   ```
3. Подождите компиляции (может занять 30-60 секунд)
4. Вы увидите:
   ```
   ▲ Next.js 15.5.6
   - Local:        http://localhost:3000
   ✓ Ready in Xs
   ```
5. Когда увидите "Ready" - сервер запущен!

### БРАУЗЕР: Откройте адрес

После того как увидите "Ready" в терминале 2, откройте браузер:

**http://localhost:3000**

---

## ✅ ПРОВЕРКА

### Проверка 1: SSH туннель работает?
```powershell
netstat -an | findstr ":5432"
```
Должно показать: `TCP    127.0.0.1:5432`

### Проверка 2: Dev сервер работает?
```powershell
netstat -an | findstr ":3000"
```
Должно показать: `TCP    0.0.0.0:3000`

---

## 🐛 ЕСЛИ НЕ РАБОТАЕТ

### Ошибка: "Cannot connect to database"
→ SSH туннель не запущен! Запустите терминал 1.

### Ошибка: "Port 3000 is already in use"
→ Завершите процесс на порту 3000 или используйте другой порт.

### Страница не открывается
→ Проверьте, что dev сервер запущен (терминал 2) и видите "Ready".

---

## 📝 БЫСТРАЯ КОМАНДА

Если хотите запустить всё автоматически, используйте готовый скрипт:

```powershell
# Терминал 1
npm run dev:tunnel

# Терминал 2 (в другом терминале!)
npm run dev
```

---

## 🎯 РЕЗУЛЬТАТ

После запуска обоих терминалов:
- ✅ SSH туннель работает (терминал 1)
- ✅ Dev сервер работает (терминал 2)
- ✅ Приложение доступно на http://localhost:3000
- ✅ Hot reload включен - изменения применяются мгновенно!

