# 🚀 БЫСТРЫЙ СТАРТ ЛОКАЛЬНОЙ РАЗРАБОТКИ

## ⚡ Запуск за 3 шага

### Шаг 1: SSH туннель (Терминал 1)

**Откройте PowerShell и выполните:**
```powershell
npm run dev:tunnel
```

> 💡 **Важно:** Оставьте этот терминал открытым! Туннель работает пока терминал открыт.

**Или вручную:**
```powershell
ssh -L 5432:localhost:5432 ubuntu@130.193.40.35
```

### Шаг 2: Dev сервер (Терминал 2)

**Откройте НОВЫЙ PowerShell и выполните:**
```powershell
npm run dev
```

### Шаг 3: Откройте браузер

Перейдите по адресу: **http://localhost:3000**

---

## ✅ Готово!

Теперь:
- ✅ **Hot reload** работает - изменения видны мгновенно
- ✅ **Реальные данные** из staging БД
- ✅ **Отладка** доступна - используйте `clientLogger` и `logger`
- ✅ **React DevTools** работают

---

## 🔧 Полезные команды

```powershell
# Проверить подключение к БД
npm run prisma:studio

# Проверить типы TypeScript
npm run type-check

# Проверить линтер
npm run lint

# Сгенерировать Prisma клиент (после изменений schema.prisma)
npm run prisma:generate
```

---

## 🐛 Troubleshooting

### Проблема: "Cannot connect to database"

**Решение:**
1. Проверьте, что SSH туннель запущен (Терминал 1)
2. Проверьте порт: `netstat -an | findstr 5432`
3. Проверьте `.env.local` - правильный ли `DATABASE_URL`?

### Проблема: "Port 5432 is already in use"

**Решение:**
1. Найдите процесс: `netstat -ano | findstr :5432`
2. Завершите процесс или используйте другой порт

### Проблема: Изменения не применяются

**Решение:**
1. Сохраните файл (Ctrl+S)
2. Проверьте терминал - нет ли ошибок компиляции?
3. Обновите страницу в браузере (Ctrl+F5)

---

## 📝 Как работать с кодом

1. **Редактируйте файлы** в VS Code или другом редакторе
2. **Сохраните файл** (Ctrl+S)
3. **Изменения применятся автоматически** - увидите в терминале и браузере
4. **Проверяйте логи** в терминале (серверные) и консоли браузера (клиентские)

---

## 🎯 Где смотреть логи

### Серверные логи (API Routes):
- **Development**: Терминал где запущен `npm run dev`
- Формат: `[ERROR] [api/route] [user:123] [session:abc...] Сообщение`

### Клиентские логи (React компоненты):
- **Development**: Консоль браузера (F12 → Console)
- Формат: `[DEBUG] Сообщение { metadata }`

Подробнее: `docs/LOGGING_GUIDE.md`

---

## ⚠️ Важные замечания

1. **НЕ используйте `prisma migrate`** для staging БД
2. **Используйте только `prisma generate`** после изменений schema.prisma
3. **Не удаляйте данные** в staging БД во время разработки
4. **Коммитьте изменения** регулярно в feature ветки

---

## 🎉 Готово к разработке!

Теперь вы можете:
- ✅ Редактировать код и видеть изменения мгновенно
- ✅ Использовать debugger и логирование
- ✅ Работать с реальными данными
- ✅ Тестировать изменения локально перед деплоем

**Приятной разработки!** 🚀

