# ⚡ БЫСТРАЯ ШПАРГАЛКА - Локальная разработка

## 🚀 За 30 секунд

```powershell
# Терминал 1: SSH туннель
npm run dev:tunnel

# Терминал 2: Dev сервер  
npm run dev

# Браузер: http://localhost:3000
```

Готово! ✅ Hot reload включен, изменения видны мгновенно!

---

## 📋 Что нужно сделать один раз

### 1. Создать `.env.local`

```env
DATABASE_URL="postgresql://staging_user:staging_password@localhost:5432/domeo_staging?schema=public"
NEXTAUTH_SECRET="local-dev-secret-change-in-production"
NEXTAUTH_URL="http://localhost:3000"
```

### 2. Установить зависимости (если еще не установлены)

```powershell
npm install
```

---

## 🔄 Ежедневный workflow

1. **Запустить SSH туннель** → `npm run dev:tunnel` (терминал 1)
2. **Запустить dev сервер** → `npm run dev` (терминал 2)
3. **Редактировать код** → изменения применяются автоматически
4. **Проверять в браузере** → `http://localhost:3000`

---

## 📚 Документация

- **Полная инструкция:** [LOCAL_DEV_SETUP.md](./LOCAL_DEV_SETUP.md)
- **Быстрый старт:** [LOCAL_DEV_QUICK_START.md](./LOCAL_DEV_QUICK_START.md)

---

## ⚠️ Важно

- ❌ **НЕ используйте `prisma migrate`** для staging БД
- ✅ После изменений в `schema.prisma` → `npm run prisma:generate`
- ✅ Оставьте терминал с SSH туннелем открытым

