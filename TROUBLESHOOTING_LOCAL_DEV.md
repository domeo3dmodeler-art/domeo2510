# 🚨 РЕШЕНИЕ: localhost:3000 не работает

## Причина: Dev сервер не запущен

Чтобы запустить dev сервер, нужно сделать следующее:

---

## 📋 Пошаговая инструкция

### Шаг 1: Запустить SSH туннель (ОБЯЗАТЕЛЬНО!)

**Откройте отдельный терминал PowerShell** и выполните:

```powershell
cd c:\01_conf\2410_deploy
npm run dev:tunnel
```

> ⚠️ **ВАЖНО:** Оставьте этот терминал открытым! Туннель работает пока терминал открыт.

**Проверка:** Вы должны увидеть, что терминал "висит" - это нормально, туннель работает.

### Шаг 2: Запустить dev сервер

**Откройте еще один терминал PowerShell** и выполните:

```powershell
cd c:\01_conf\2410_deploy
npm run dev
```

**Что должно произойти:**
- Увидите процесс компиляции Next.js
- В конце увидите: `▲ Next.js 15.5.6` и `- Local: http://localhost:3000`
- Сервер запустится и будет ждать изменений

### Шаг 3: Открыть браузер

После того как увидите сообщение `Local: http://localhost:3000`, откройте браузер и перейдите по адресу:

**http://localhost:3000**

---

## 🔍 Проверка статуса

### Проверка 1: SSH туннель запущен?

```powershell
netstat -an | findstr ":5432"
```

Должно показать: `TCP    127.0.0.1:5432`

Если не показывает - туннель не запущен!

### Проверка 2: Dev сервер запущен?

```powershell
netstat -an | findstr ":3000"
```

Должно показать: `TCP    0.0.0.0:3000` или `TCP    127.0.0.1:3000`

Если не показывает - сервер не запущен!

### Проверка 3: Браузер открывает страницу?

Если сервер запущен, но браузер показывает ошибку:
- Проверьте, что используете правильный адрес: `http://localhost:3000` (не `https://`)
- Попробуйте другой браузер
- Проверьте консоль браузера (F12) на наличие ошибок

---

## ⚠️ Частые ошибки

### Ошибка: "Cannot connect to database"

**Причина:** SSH туннель не запущен

**Решение:** 
1. Запустите SSH туннель: `npm run dev:tunnel` (в отдельном терминале)
2. Подождите 2-3 секунды
3. Перезапустите dev сервер

### Ошибка: "Port 3000 is already in use"

**Причина:** Другой процесс использует порт 3000

**Решение:**
```powershell
# Найти процесс
netstat -ano | findstr :3000

# Завершить процесс (замените PID на номер из команды выше)
taskkill /PID <PID> /F
```

### Ошибка: "Module not found" или ошибки компиляции

**Решение:**
```powershell
# Переустановить зависимости
npm install

# Сгенерировать Prisma клиент
npm run prisma:generate

# Перезапустить dev сервер
npm run dev
```

---

## ✅ Быстрая проверка

Выполните все команды по порядку:

```powershell
# 1. Перейти в директорию проекта
cd c:\01_conf\2410_deploy

# 2. Проверить .env.local
Get-Content .env.local | Select-String "DATABASE_URL"

# 3. Запустить SSH туннель (в отдельном терминале!)
npm run dev:tunnel

# 4. Запустить dev сервер (в другом терминале!)
npm run dev
```

---

## 🎯 Итог

Чтобы `http://localhost:3000` работал, нужно:

1. ✅ SSH туннель запущен (`npm run dev:tunnel`)
2. ✅ Dev сервер запущен (`npm run dev`)
3. ✅ Открыть браузер на `http://localhost:3000`

Если все сделано правильно, но не работает - проверьте ошибки в терминале где запущен `npm run dev`.

