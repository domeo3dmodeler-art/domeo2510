{
  "name": "domeo-configurators",
  "version": "1.0.0",
  "description": "NoCode платформа конфигураторов для разных групп товаров",
  "scripts": {
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build",
    "start": "cd app && npm run start",
    "install-deps": "cd app && npm install",
    "setup": "npm run install-deps && npm run dev"
  },
  "keywords": [
    "nocode",
    "configurator",
    "ecommerce",
    "domeo"
  ],
  "author": "Domeo Team",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^6.17.1",
    "lucide-react": "^0.545.0",
    "prisma": "^6.17.1",
    "xlsx": "^0.18.5"
  }
}
