# Скрипт проверки категорий "Освещение"

## Описание

Скрипт проверяет наличие категорий "Освещение" в базе данных и сравнивает их с категориями из YML файла `app/light/all.yml`.

## Использование

### Запуск скрипта:

```bash
npm run check:light-categories
```

### Требования:

1. **Переменная окружения `DATABASE_URL`** должна быть установлена
   - Проверьте файл `.env` или `.env.local`
   - Формат: `postgresql://user:password@host:port/database`

### Пример:

```bash
# Убедитесь, что DATABASE_URL установлена
export DATABASE_URL="postgresql://user:password@localhost:5432/domeo"

# Запустите скрипт
npm run check:light-categories
```

## Что делает скрипт:

1. **Проверяет корневую категорию "Освещение"**
   - Ищет категорию с названием "Освещение", "Light", "освещение" или "light"
   - Показывает количество товаров и подкатегорий

2. **Ищет все категории освещения**
   - Категории, которые являются подкатегориями "Освещение"
   - Категории, названия которых совпадают с категориями из YML

3. **Сравнивает с YML файлом**
   - Показывает, какие категории найдены в БД
   - Показывает, какие категории отсутствуют в БД
   - Группирует отсутствующие категории по типам

4. **Выводит статистику**
   - Всего категорий в YML
   - Найдено в БД
   - Отсутствует в БД
   - Процент покрытия

## Вывод скрипта:

Скрипт выводит:
- ✅ Найденные категории в БД
- ❌ Категории, которые нужно добавить
- 📊 Статистику по категориям
- 📋 Группировку отсутствующих категорий

## Пример вывода:

```
🔍 Проверка категорий "Освещение" в базе данных...

📌 Шаг 1: Поиск корневой категории "Освещение"...

✅ Найдено корневых категорий: 1
   - "Освещение" (ID: clx..., level: 0)
     Товаров: 0, Подкатегорий: 15

📌 Шаг 2: Поиск всех категорий, связанных с освещением...

✅ Найдено категорий в БД: 15

📊 РЕЗУЛЬТАТЫ ПРОВЕРКИ

✅ Найдено в БД: 15 категорий
❌ НЕ найдено в БД: 52 категории

📈 СТАТИСТИКА:
   Всего категорий в YML: 67
   Найдено в БД: 15
   Отсутствует в БД: 52
   Процент покрытия: 22.4%
```

## Устранение неполадок:

### Ошибка: "Environment variable not found: DATABASE_URL"

Решение:
1. Создайте файл `.env` в корне проекта
2. Добавьте строку: `DATABASE_URL="postgresql://user:password@host:port/database"`
3. Замените значения на ваши реальные данные БД

### Ошибка подключения к БД

Решение:
1. Проверьте, что PostgreSQL запущен
2. Проверьте правильность DATABASE_URL
3. Проверьте доступность БД с вашего компьютера
