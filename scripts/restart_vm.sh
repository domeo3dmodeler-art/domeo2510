#!/usr/bin/env bash
# Скрипт для перезапуска приложения на ВМ

set -euo pipefail

echo "🔄 Перезапуск сервиса domeo на ВМ..."

# Перезапуск systemd сервиса
sudo systemctl restart domeo

# Проверка статуса
echo "📊 Статус сервиса:"
sudo systemctl status domeo --no-pager -l

echo "✅ Сервис перезапущен!"

