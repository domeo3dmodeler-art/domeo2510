# 🚀 БЫСТРЫЙ СТАРТ ЛОКАЛЬНОЙ РАЗРАБОТКИ

## ✅ ПРАВИЛЬНЫЙ ВАРИАНТ: SSH туннель к staging БД

Этот вариант **рекомендуется**, потому что:
- ✅ Используете реальные данные из staging
- ✅ Не нужно создавать локальную БД
- ✅ Безопасно через SSH
- ✅ Быстрый старт за 2 минуты

---

## 📋 Пошаговая инструкция

### Шаг 1: Создать SSH туннель (в отдельном терминале PowerShell)

```powershell
ssh -L 5432:localhost:5432 ubuntu@130.193.40.35
```

> 💡 **Важно:** Оставьте этот терминал открытым - туннель работает пока терминал открыт

### Шаг 2: Создать `.env.local` в корне проекта

```env
DATABASE_URL="postgresql://staging_user:staging_password@localhost:5432/domeo_staging?schema=public"
NEXTAUTH_SECRET="local-dev-secret-change-in-production"
NEXTAUTH_URL="http://localhost:3000"
```

### Шаг 3: Запустить dev сервер

```powershell
npm run dev
```

Откройте браузер: `http://localhost:3000`

---

## 🔥 Преимущества

- ⚡ **Hot reload** - изменения видны мгновенно без пересборки Docker
- 🔄 **Быстрая итерация** - редактируете код → сразу видите результат
- 🐛 **Отладка** - можете использовать debugger, console.log, React DevTools
- 📊 **Реальные данные** - работаете с данными из staging

---

## 🛠️ Команды для разработки

```powershell
# Запустить dev сервер
npm run dev

# Сгенерировать Prisma клиент после изменений schema
npm run prisma:generate

# Открыть Prisma Studio (GUI для БД)
npm run prisma:studio

# Проверить типы TypeScript
npm run type-check

# Проверить линтер
npm run lint
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **НЕ используйте `prisma migrate` для staging БД** - это может повредить данные
2. **Для изменений схемы** используйте только `prisma generate` - миграции применяются на ВМ при деплое
3. **После изменений в schema.prisma** всегда запускайте `npm run prisma:generate`
4. **Не удаляйте данные** в staging БД во время разработки

---

## 🔧 Troubleshooting

### Проблема: Не могу подключиться к БД

**Проверьте SSH туннель:**
```powershell
# Проверьте, что туннель работает
netstat -an | findstr 5432
```

**Если туннель не работает:**
1. Убедитесь, что терминал с SSH туннелем открыт
2. Проверьте подключение к ВМ: `ssh ubuntu@130.193.40.35`
3. Перезапустите туннель

### Проблема: Prisma клиент не обновился

```powershell
npm run prisma:generate
# Перезапустите dev сервер (Ctrl+C и npm run dev)
```

---

## 🎯 Workflow разработки

1. **Откройте SSH туннель** (в отдельном терминале)
2. **Запустите `npm run dev`**
3. **Редактируйте код** → изменения применяются автоматически
4. **Проверяйте результат** в браузере
5. **После завершения** → коммитьте и деплойте на staging

---

## 📝 Автоматический запуск (рекомендуется)

Используйте готовый скрипт:

```powershell
npm run dev:quick
```

Скрипт автоматически:
- Проверит наличие `.env.local`
- Предложит создать его, если отсутствует
- Установит зависимости
- Сгенерирует Prisma клиент
- Запустит dev сервер

---

## ❌ ВАРИАНТЫ, КОТОРЫЕ НЕ РЕКОМЕНДУЮТСЯ

### ❌ Вариант А: Локальная PostgreSQL через Docker
- ❌ Нужно создавать локальную БД
- ❌ Нет реальных данных
- ❌ Дополнительная настройка

### ❌ Вариант В: Прямое подключение к staging БД
- ❌ Небезопасно (нет SSH шифрования)
- ❌ Может требовать открытия портов на ВМ
- ❌ Зависит от сетевых настроек

---

## ✅ ИТОГО: Правильный вариант

**SSH туннель к staging БД** - это единственный правильный вариант для быстрой разработки.

```powershell
# 1. SSH туннель (отдельный терминал)
ssh -L 5432:localhost:5432 ubuntu@130.193.40.35

# 2. .env.local
DATABASE_URL="postgresql://staging_user:staging_password@localhost:5432/domeo_staging?schema=public"

# 3. Запуск
npm run dev
```

Готово! Теперь у вас есть hot reload и быстрая разработка! 🚀
