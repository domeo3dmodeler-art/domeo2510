# 🚀 Быстрый старт локальной разработки

## ⚡ Вариант 1: Локальная БД (Рекомендуется)

### Быстрый старт:

```powershell
# 1. Установить зависимости (только один раз)
npm install

# 2. Создать .env.local с локальной БД или подключением к staging
# См. ниже варианты подключения

# 3. Запустить dev сервер с hot reload
npm run dev
```

### Настройка .env.local:

**Вариант А: Локальная PostgreSQL (Docker)**
```env
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/domeo_local?schema=public"
```

**Вариант Б: Подключение к staging БД через SSH туннель**
```env
# В .env.local используйте SSH туннель:
# Сначала создайте туннель:
# ssh -L 5433:localhost:5432 ubuntu@130.193.40.35

DATABASE_URL="postgresql://staging_user:staging_password@localhost:5433/domeo_staging?schema=public"
```

**Вариант В: Использовать staging БД напрямую (не рекомендуется для разработки)**
```env
DATABASE_URL="postgresql://staging_user:staging_password@130.193.40.35:5432/domeo_staging?schema=public"
```

## 🔥 Вариант 2: SSH туннель к staging БД (Быстро, но используйте осторожно)

### Шаг 1: Создать SSH туннель
```powershell
# В отдельном терминале PowerShell
ssh -L 5433:localhost:5432 ubuntu@130.193.40.35
```

### Шаг 2: Создать .env.local
```env
DATABASE_URL="postgresql://staging_user:staging_password@localhost:5433/domeo_staging?schema=public"
NEXTAUTH_SECRET="your-secret-key"
NEXTAUTH_URL="http://localhost:3000"
```

### Шаг 3: Запустить dev сервер
```powershell
npm run dev
```

Приложение будет доступно на `http://localhost:3000` с **hot reload** - изменения применяются мгновенно!

## 🎯 Преимущества локальной разработки:

✅ **Мгновенный hot reload** - изменения видны сразу без пересборки  
✅ **Быстрая итерация** - редактируйте код и сразу видите результат  
✅ **Отладка** - можно использовать debugger и console.log  
✅ **Не влияет на staging** - можете экспериментировать безопасно  

## 📝 Команды для разработки:

```powershell
# Запустить dev сервер
npm run dev

# Сгенерировать Prisma клиент после изменений schema
npm run prisma:generate

# Применить изменения схемы (только для локальной БД!)
npm run prisma:push

# Открыть Prisma Studio (GUI для БД)
npm run prisma:studio

# Проверить типы TypeScript
npm run type-check

# Проверить линтер
npm run lint
```

## ⚠️ Важные замечания:

1. **Не используйте `prisma migrate` для staging БД** - используйте только для локальной БД
2. **Для staging БД используйте только чтение** или создавайте тестовые данные
3. **После изменений в schema.prisma** всегда запускайте `npm run prisma:generate`

## 🔧 Troubleshooting:

**Проблема: Не могу подключиться к БД**
```powershell
# Проверьте SSH туннель:
netstat -an | findstr 5433

# Или проверьте Docker контейнер:
docker ps | findstr postgres
```

**Проблема: Prisma клиент не обновился**
```powershell
npm run prisma:generate
# Перезапустите dev сервер (Ctrl+C и npm run dev)
```

