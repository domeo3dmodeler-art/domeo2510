# ❌ Проблема: localhost:3000 не загружается

## 🔍 Диагностика проблемы:

**Проблема:** Зависимости не установлены полностью
- Next.js не найден в `node_modules`
- Prisma файлы заблокированы (EBUSY)
- Ошибка сети при установке Prisma engines

---

## ✅ РЕШЕНИЕ: Установка зависимостей вручную

### Шаг 1: Закройте все процессы Node.js

**В PowerShell выполните:**
```powershell
Get-Process | Where-Object {$_.ProcessName -eq "node"} | Stop-Process -Force
```

**Или закройте все окна PowerShell с запущенными процессами.**

### Шаг 2: Установите зависимости

**В PowerShell выполните:**
```powershell
cd C:\01_conf\0411
npm install
```

> ⏳ Это может занять 5-10 минут в зависимости от скорости интернета.

**Если Prisma все еще блокирован, попробуйте:**
```powershell
# Вариант 1: Установка без Prisma postinstall
npm install --ignore-scripts

# Вариант 2: Установка с retry
npm install --max-retries=5
```

### Шаг 3: Запустите SSH туннель (Терминал 1)

**Откройте PowerShell и выполните:**
```powershell
npm run dev:tunnel
```

> 💡 Оставьте этот терминал открытым!

### Шаг 4: Запустите dev сервер (Терминал 2)

**Откройте НОВЫЙ PowerShell и выполните:**
```powershell
npm run dev
```

Вы должны увидеть:
```
  ▲ Next.js 15.5.6
  - Local:        http://localhost:3000
  - Ready in X.Xs
```

### Шаг 5: Откройте браузер

Перейдите на: **http://localhost:3000**

---

## 🔧 Альтернативное решение (если npm install не работает)

### Вариант 1: Очистка и переустановка

```powershell
# Удалите node_modules и package-lock.json
Remove-Item -Recurse -Force node_modules
Remove-Item -Force package-lock.json

# Установите заново
npm install
```

### Вариант 2: Использование yarn (если установлен)

```powershell
yarn install
yarn dev
```

### Вариант 3: Проверка антивируса

Если Prisma файлы заблокированы - возможно антивирус блокирует доступ.
Попробуйте временно отключить антивирус или добавить папку проекта в исключения.

---

## 🐛 Если все еще не работает

### Проверьте ошибки в терминале:

1. **Откройте терминал где запущен `npm run dev`**
2. **Посмотрите на ошибки** - они покажут что не так
3. **Скопируйте ошибку** и сообщите мне

### Частые ошибки:

**"Cannot find module 'next'"**
→ Зависимости не установлены → выполните `npm install`

**"Port 3000 is already in use"**
→ Порт занят → найдите процесс: `netstat -ano | findstr :3000` и завершите его

**"ECONNREFUSED" или "Cannot connect to database"**
→ SSH туннель не запущен → запустите `npm run dev:tunnel`

**"Prisma Client not found"**
→ Выполните: `npm run prisma:generate`

---

## 📋 Чек-лист для проверки:

- [ ] Зависимости установлены (`npm install` выполнен без ошибок)
- [ ] SSH туннель запущен (отдельный терминал)
- [ ] Dev сервер запущен (отдельный терминал, показывает Next.js)
- [ ] Порт 3000 слушается (`netstat -an | findstr :3000`)
- [ ] Браузер открывает http://localhost:3000

---

## 🚀 Быстрая команда для запуска:

```powershell
# 1. Установите зависимости (если еще не установлены)
npm install

# 2. Терминал 1: SSH туннель
npm run dev:tunnel

# 3. Терминал 2: Dev сервер
npm run dev

# 4. Браузер: http://localhost:3000
```

---

**После выполнения этих шагов проект должен запуститься!** 🎉

Если проблемы остаются - пришлите ошибку из терминала где запущен `npm run dev`.

