# ✅ HOT RELOAD НАСТРОЕН И ЗАПУЩЕН!

## 🎉 Статус: ГОТОВО

**Дата настройки:** 2025-11-05

---

## ✅ Что сделано:

1. ✅ Создан `docker-compose.staging-dev.yml` с hot reload конфигурацией
2. ✅ Файлы скопированы на staging ВМ (`/opt/domeo`)
3. ✅ Старые контейнеры остановлены
4. ✅ Новые контейнеры запущены в hot reload режиме
5. ✅ Контейнер перезапущен с исправленной конфигурацией

---

## 🌐 Доступ:

**Staging URL:** http://130.193.40.35:3001

---

## 🔄 Как использовать:

### Быстрая синхронизация кода:

```powershell
# 1. Синхронизировать директории
scp -r app ubuntu@130.193.40.35:/opt/domeo/
scp -r components ubuntu@130.193.40.35:/opt/domeo/
scp -r lib ubuntu@130.193.40.35:/opt/domeo/

# 2. Перезапустить контейнер для применения изменений
ssh ubuntu@130.193.40.35 'cd /opt/domeo && docker compose -f docker-compose.staging-dev.yml restart staging-app'
```

### Просмотр логов:

```bash
ssh ubuntu@130.193.40.35 'docker logs -f domeo-staging-app'
```

---

## 📊 Управление:

### Статус контейнеров:
```bash
ssh ubuntu@130.193.40.35 'docker ps | grep staging'
```

### Перезапуск:
```bash
ssh ubuntu@130.193.40.35 'cd /opt/domeo && docker compose -f docker-compose.staging-dev.yml restart staging-app'
```

### Остановка:
```bash
ssh ubuntu@130.193.40.35 'cd /opt/domeo && docker compose -f docker-compose.staging-dev.yml down'
```

---

## ⚡ Преимущества:

- ✅ **Мгновенные изменения** - без пересборки Docker образа (5-15 минут → 30 секунд)
- ✅ **Hot Reload** - Next.js автоматически перезагружает страницу при изменениях
- ✅ **Быстрая разработка** - видите изменения сразу
- ✅ **Реальные данные** - используете staging БД

---

## 📝 Примечания:

1. **Первый запуск** может занять 2-5 минут (установка зависимостей)
2. **Prisma Client** генерируется автоматически при первом запуске
3. **node_modules** сохраняется в volume - не переустанавливается каждый раз
4. **.next кэш** сохраняется - ускоряет перезапуски

---

## 🔧 Структура на staging ВМ:

```
/opt/domeo/
├── docker-compose.staging-dev.yml  # Конфигурация hot reload
├── app/                             # Монтируется для hot reload
├── components/                      # Монтируется для hot reload
├── lib/                            # Монтируется для hot reload
└── ...
```

---

**Готово к использованию!** 🚀

Изменения применяются мгновенно без пересборки Docker образа!

